"use client"

/** `/workflows/new` — the builder. See `NEW_WORKFLOW_HREF` in lib/workflows.ts. */

import { NewWorkflowView } from "@/components/layout/new-workflow-view"

export default function NewWorkflowPage() {
  return <NewWorkflowView />
}
